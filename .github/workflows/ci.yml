name: CI

on:
  push:
    branches:
      - '**'
    tags:
      - '[0-9]+.*'
  pull_request:
  workflow_dispatch:

jobs:
  lint:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout last commit
        uses: actions/checkout@v4

      - name: Install clang-format 18
        if: ${{ !startsWith(github.ref, 'refs/tags/') }}
        run: |
          wget https://apt.llvm.org/llvm-snapshot.gpg.key
          sudo gpg --dearmor < llvm-snapshot.gpg.key > /usr/share/keyrings/llvm-archive-keyring.gpg
          echo "deb [signed-by=/usr/share/keyrings/llvm-archive-keyring.gpg] http://apt.llvm.org/jammy/ llvm-toolchain-jammy-18 main" | sudo tee /etc/apt/sources.list.d/llvm-toolchain-jammy-18.list
          sudo apt-get -o Acquire::ForceIPv4=true -o Acquire::Retries=3 update
          sudo apt-get -o Acquire::ForceIPv4=true -o Acquire::Retries=3 install -y clang-format-18
          sudo ln -sf /usr/bin/clang-format-18 /usr/local/bin/clang-format
          clang-format --version

      - name: Code style lint
        if: ${{ !startsWith(github.ref, 'refs/tags/') }}
        shell: bash
        run: ./clang-format.sh -i

      - name: Verify settings layout contract
        if: ${{ !startsWith(github.ref, 'refs/tags/') }}
        shell: pwsh
        run: ./WeaselDeployer/tests/CheckSettingsLayout.ps1

  build:
    needs: lint
    runs-on: windows-2022
    env:
      boost_version: 1.84.0
      BOOST_ROOT: ${{ github.workspace }}\deps\boost_1_84_0
      SDKVER: 10.0.19041.0
    strategy:
      matrix:
        variant: [msbuild, xmake]
    steps:
      - name: Checkout last commit
        uses: actions/checkout@v4
        with:
          submodules: true
          fetch-depth: 0

      - name: Fetch all tags
        run: git fetch --tags

      - name: Verify shared version numbering
        if: matrix.variant == 'msbuild'
        shell: pwsh
        run: .\tools\tests\CheckVersioning.ps1

      - name: Configure build environment
        shell: bash
        run: |
          cp env.vs2022.bat env.bat
          echo git_ref_name="$(git describe --always)" >> $GITHUB_ENV

      - name: Cache Boost
        id: cache-boost
        uses: actions/cache@v4
        with:
          path: |
            ${{ env.BOOST_ROOT }}
          key: ${{ runner.os }}-boost-${{ env.boost_version }}

      - name: Install Boost
        if: steps.cache-boost.outputs.cache-hit != 'true'
        shell: bash
        run: |
          ./install_boost.bat
          ./build.bat boost arm64

      - uses: ilammy/msvc-dev-cmd@v1
        with:
          sdk: ${{ env.SDKVER }}

      - name: Test model installation and recovery
        if: matrix.variant == 'msbuild'
        shell: pwsh
        run: .\WeaselDeployer\tests\RunModelTransactionTests.ps1

      - name: Test input method icon resources and profile rollback
        if: matrix.variant == 'msbuild'
        shell: pwsh
        run: .\WeaselDeployer\tests\RunInputMethodIconTests.ps1

      # bundled winsparkle v0.9.2 to disable auto open ReleaseNoteUrl in default browser
      - name: cache bundled winsparkle
        id: cache-winsparkle
        uses: actions/cache@v4
        with:
          path: |
            ${{ github.workspace }}\winsparkle\x64\Release\WinSparkle.dll
            ${{ github.workspace }}\winsparkle\x64\Release\WinSparkle.lib
            ${{ github.workspace }}\winsparkle\Release\WinSparkle.dll
            ${{ github.workspace }}\winsparkle\Release\WinSparkle.lib
          key: ${{ runner.os }}-winsparkle-v0.9.2-bundled
          restore-keys: |
            winsparkle-v0.9.2-bundled

      - name: Build bundled winsparkle
        if: steps.cache-winsparkle.outputs.cache-hit != 'true'
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          if (Test-Path winsparkle) { Remove-Item -Recurse -Force winsparkle }
          # Clone winsparkle repo v0.9.2
          git clone https://github.com/vslavik/winsparkle.git -b v0.9.2 --depth 1
          cd winsparkle
          git submodule update --init --recursive
          copy ..\bundled_winsparkle.patch .
          git apply bundled_winsparkle.patch
          nuget restore
          msbuild -m -t:Build -p:Configuration=Release -p:Platform=x64 WinSparkle.sln
          msbuild -m -t:Build -p:Configuration=Release -p:Platform=Win32 WinSparkle.sln

      - name: Install WinSparkle files
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          copy .\winsparkle\x64\Release\WinSparkle.dll .\output\
          copy .\winsparkle\x64\Release\WinSparkle.lib .\lib64\
          copy .\winsparkle\Release\WinSparkle.dll .\output\Win32\
          copy .\winsparkle\Release\WinSparkle.lib .\lib\

      - name: Get Rime files
        shell: pwsh
        env:
          RIME_GITHUB_TOKEN: ${{ github.token }}
        run: |
          $ErrorActionPreference = 'Stop'
          $global:authorization = $env:RIME_GITHUB_TOKEN
          .\get-rime.ps1 -use dev

      - name: Verify Rime development files
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          foreach ($requiredFile in @('include\rime_api.h', 'include\rime_levers_api.h', 'lib\rime.lib', 'lib64\rime.lib', 'output\Win32\rime.dll', 'output\rime.dll')) {
            if (-not (Test-Path -LiteralPath $requiredFile -PathType Leaf)) {
              throw "Rime dependency download incomplete: missing $requiredFile"
            }
            if ((Get-Item -LiteralPath $requiredFile).Length -eq 0) {
              throw "Rime dependency download incomplete: empty $requiredFile"
            }
          }

      - name: Build data
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          .\build.bat data
          if ($LASTEXITCODE -ne 0) { throw "Data build failed with exit code $LASTEXITCODE" }

      - name: Setup xmake
        if: matrix.variant == 'xmake'
        uses: xmake-io/github-action-setup-xmake@v1

      - name: Build Windows App SDK Acrylic helper (x64)
        if: matrix.variant == 'xmake'
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          msbuild .\AcrylicAppSdkPoC\AcrylicAppSdkPoC.vcxproj /t:Restore /p:Configuration=Release /p:Platform=x64
          if ($LASTEXITCODE -ne 0) { throw 'Acrylic x64 helper restore failed' }
          msbuild .\AcrylicAppSdkPoC\AcrylicAppSdkPoC.vcxproj /m /p:Configuration=Release /p:Platform=x64
          if ($LASTEXITCODE -ne 0) { throw 'Acrylic x64 helper build failed' }

      - name: Test synchronous Acrylic child target (x64 and x86)
        if: matrix.variant == 'xmake'
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          foreach ($testPlatform in @('x64', 'Win32')) {
            msbuild .\AcrylicAppSdkPoC\tests\ChildBackdropTargetTests.vcxproj /m /p:Configuration=Release "/p:Platform=$testPlatform"
            if ($LASTEXITCODE -ne 0) { throw "Child target tests failed to build: $testPlatform" }
            $testDirectory = Join-Path $PWD "AcrylicAppSdkPoC\tests\bin\$testPlatform"
            $testProcess = Start-Process -FilePath (Join-Path $testDirectory 'ChildBackdropTargetTests.exe') -WindowStyle Hidden -PassThru -RedirectStandardOutput (Join-Path $testDirectory 'stdout.txt') -RedirectStandardError (Join-Path $testDirectory 'stderr.txt')
            if (-not $testProcess.WaitForExit(20000)) {
              $testProcess.Kill()
              $testProcess.WaitForExit()
              throw "Child target tests timed out: $testPlatform"
            }
            Get-Content -LiteralPath (Join-Path $testDirectory 'stdout.txt'), (Join-Path $testDirectory 'stderr.txt')
            if ($testProcess.ExitCode -ne 0) { throw "Child target tests failed: $testPlatform" }
          }

      - name: Test TSF composition cleanup (x64 and x86)
        if: matrix.variant == 'xmake'
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          .\WeaselTSF\tests\PrepareCompositionTests.ps1
          foreach ($testPlatform in @('x64', 'Win32')) {
            msbuild .\WeaselTSF\tests\CompositionLifecycleTests.vcxproj /m /p:Configuration=Release "/p:Platform=$testPlatform"
            if ($LASTEXITCODE -ne 0) { throw "Composition tests failed to build: $testPlatform" }
            $testDirectory = Join-Path $PWD "WeaselTSF\tests\bin\$testPlatform"
            $testProcess = Start-Process -FilePath (Join-Path $testDirectory 'CompositionLifecycleTests.exe') -WindowStyle Hidden -PassThru -RedirectStandardOutput (Join-Path $testDirectory 'stdout.txt') -RedirectStandardError (Join-Path $testDirectory 'stderr.txt')
            if (-not $testProcess.WaitForExit(20000)) {
              $testProcess.Kill()
              $testProcess.WaitForExit()
              throw "Composition tests timed out: $testPlatform"
            }
            Get-Content -LiteralPath (Join-Path $testDirectory 'stdout.txt'), (Join-Path $testDirectory 'stderr.txt')
            if ($testProcess.ExitCode -ne 0) { throw "Composition tests failed: $testPlatform" }
          }

      - name: Test Acrylic material pixels and theme changes (x64 and x86)
        if: matrix.variant == 'xmake'
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          foreach ($testPlatform in @('x64', 'Win32')) {
            msbuild .\AcrylicAppSdkPoC\tests\AcrylicMaterialTests.vcxproj /t:Restore /p:Configuration=Release "/p:Platform=$testPlatform"
            if ($LASTEXITCODE -ne 0) { throw "Material test restore failed: $testPlatform" }
            msbuild .\AcrylicAppSdkPoC\tests\AcrylicMaterialTests.vcxproj /m /p:Configuration=Release "/p:Platform=$testPlatform"
            if ($LASTEXITCODE -ne 0) { throw "Material test build failed: $testPlatform" }
            $testDirectory = Join-Path $PWD "AcrylicAppSdkPoC\tests\bin\material\$testPlatform"
            $testProcess = Start-Process -FilePath (Join-Path $testDirectory 'AcrylicMaterialTests.exe') -WindowStyle Hidden -PassThru -RedirectStandardOutput (Join-Path $testDirectory 'stdout.txt') -RedirectStandardError (Join-Path $testDirectory 'stderr.txt')
            if (-not $testProcess.WaitForExit(30000)) {
              $testProcess.Kill()
              $testProcess.WaitForExit()
              throw "Material tests timed out: $testPlatform"
            }
            Get-Content -LiteralPath (Join-Path $testDirectory 'stdout.txt'), (Join-Path $testDirectory 'stderr.txt')
            if ($testProcess.ExitCode -ne 0) { throw "Material tests failed: $testPlatform" }
          }

      - name: Prepare and verify Acrylic runtime pair (x64)
        if: matrix.variant == 'xmake'
        shell: pwsh
        run: .\AcrylicAppSdkPoC\verify-runtime.ps1 -Architecture x64

      - name: Build Windows App SDK Acrylic helper (x86)
        if: matrix.variant == 'xmake'
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          msbuild .\AcrylicAppSdkPoC\AcrylicAppSdkPoC.vcxproj /t:Restore /p:Configuration=Release /p:Platform=Win32
          if ($LASTEXITCODE -ne 0) { throw 'Acrylic x86 helper restore failed' }
          msbuild .\AcrylicAppSdkPoC\AcrylicAppSdkPoC.vcxproj /m /p:Configuration=Release /p:Platform=Win32
          if ($LASTEXITCODE -ne 0) { throw 'Acrylic x86 helper build failed' }

      - name: Prepare and verify Acrylic runtime pair (x86)
        if: matrix.variant == 'xmake'
        shell: pwsh
        run: .\AcrylicAppSdkPoC\verify-runtime.ps1 -Architecture x86

      - name: Build Weasel
        shell: pwsh
        id: build_weasel
        env:
          RELEASE_BUILD: ${{ (github.repository == 'rime/weasel' && startsWith(github.ref, 'refs/tags/')) && '1' || '' }}
        run: |
          $ErrorActionPreference = 'Stop'
          if ('${{ matrix.variant }}' -eq 'msbuild') {
            .\build.bat arm64 installer
          } else {
            .\xbuild.bat arm64 installer
          }

      - name: Compress Debug Symbols
        if: matrix.variant == 'msbuild'
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          output\7z.exe a -t7z "./output/archives/debug_symbols.7z" "output/*.pdb" -r

      - name: Verify Acrylic DLLs in xmake installer
        if: matrix.variant == 'xmake'
        shell: pwsh
        run: .\AcrylicAppSdkPoC\verify-runtime.ps1 -VerifyInstaller

      - name: Upload WinAppSDK Acrylic helper artifact
        if: matrix.variant == 'xmake'
        uses: actions/upload-artifact@v4
        with:
          name: weasel-acrylic-appsdk-helper-${{ env.git_ref_name }}
          if-no-files-found: error
          path: |
            .\output\WeaselAcrylicAppSdk.dll
            .\output\Microsoft.WindowsAppRuntime.Bootstrap.dll
            .\output\acrylic\x86\WeaselAcrylicAppSdk.dll
            .\output\acrylic\x86\Microsoft.WindowsAppRuntime.Bootstrap.dll
            .\output\acrylic-runtime-x64.json
            .\output\acrylic-runtime-x86.json

      - name: Upload xmake installer artifact
        if: matrix.variant == 'xmake'
        uses: actions/upload-artifact@v4
        with:
          name: weasel-xmake-acrylic-${{ env.git_ref_name }}
          if-no-files-found: error
          path: |
            .\output\archives\weasel*.exe
      - name: Upload artifacts
        if: matrix.variant == 'msbuild'
        uses: actions/upload-artifact@v4
        with:
          name: weasel-artifact-${{ env.git_ref_name }}
          if-no-files-found: error
          path: |
            .\output\archives\weasel*.exe
            .\output\archives\debug_symbols.7z

      # Release-only steps (run on tags or master)
      - name: Extract changelog
        if: ${{ github.repository == 'rime/weasel' && startsWith(github.ref, 'refs/tags/') && matrix.variant == 'msbuild' }}
        shell: pwsh
        run: .\extract_changelog.ps1

      - name: Create Nightly release
        if: ${{ github.repository == 'rime/weasel' && github.ref == 'refs/heads/master' && matrix.variant == 'msbuild' }}
        uses: 'marvinpinto/action-automatic-releases@latest'
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          automatic_release_tag: latest
          prerelease: true
          title: "Nightly Build"
          files: |
            ./output/archives/weasel*.exe
            ./output/archives/debug_symbols.7z

      - name: Release (stable)
        if: ${{ github.repository == 'rime/weasel' && startsWith(github.ref, 'refs/tags/') && matrix.variant == 'msbuild' }}
        uses: softprops/action-gh-release@v2
        with:
          draft: true
          files: |
            ./output/archives/weasel*.exe
            ./output/archives/debug_symbols.7z
          body_path: ${{ github.workspace }}/RELEASE_CHANGELOG.md

  release_fork:
    if: ${{ github.repository == 'nbzQing/weasel' && startsWith(github.ref, 'refs/tags/') }}
    needs: build
    runs-on: ubuntu-22.04
    permissions:
      contents: write
    steps:
      - name: Checkout release commit
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download verified xmake installer
        uses: actions/download-artifact@v4
        with:
          pattern: weasel-xmake-acrylic-*
          path: output/archives
          merge-multiple: true

      - name: Prepare installer, checksum, and updater feed
        shell: pwsh
        env:
          RELEASE_TAG: ${{ github.ref_name }}
        run: |
          $ErrorActionPreference = 'Stop'
          $commitCount = [int](& git rev-list HEAD --count)
          if ($LASTEXITCODE -ne 0) { throw 'Unable to count release commits.' }
          $expectedVersion = "0.17.4.$(193 + $commitCount)"
          if ($env:RELEASE_TAG -ne $expectedVersion) {
            throw "Release tag $env:RELEASE_TAG does not match build version $expectedVersion."
          }
          $installers = @(Get-ChildItem -LiteralPath output/archives -Filter 'weasel*.exe' -File)
          if ($installers.Count -ne 1 -or $installers[0].Length -le 0) {
            throw 'Expected exactly one nonempty xmake installer artifact.'
          }
          $shortSha = (& git rev-parse --short HEAD).Trim()
          if ($LASTEXITCODE -ne 0) { throw 'Unable to read release commit.' }
          $installerName = "weasel-official-0.17.4-custom-$env:RELEASE_TAG.$shortSha-installer.exe"
          $installerPath = Join-Path output/archives $installerName
          Move-Item -LiteralPath $installers[0].FullName -Destination $installerPath
          $installer = Get-Item -LiteralPath $installerPath
          $checksum = (Get-FileHash -LiteralPath $installerPath -Algorithm SHA256).Hash.ToLowerInvariant()
          [IO.File]::WriteAllText((Join-Path $PWD 'output/archives/SHA256SUMS.txt'),
            "$checksum  $installerName`n", [Text.UTF8Encoding]::new($false))
          ./tools/New-Appcast.ps1 -Version $env:RELEASE_TAG -InstallerName $installerName -InstallerSize $installer.Length
          Write-Host "Release installer: $installerName ($($installer.Length) bytes)"

      - name: Publish fork release
        uses: softprops/action-gh-release@v2
        with:
          draft: false
          prerelease: false
          name: 小狼毫图形化改版 ${{ github.ref_name }}（基于官方 0.17.4）
          body_path: .github/RELEASE_NOTES_CN.md
          files: |
            output/archives/weasel-official-0.17.4-custom-*-installer.exe
            output/archives/SHA256SUMS.txt
            output/archives/appcast.xml
