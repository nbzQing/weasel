# 亚克力磨砂与外观设置

任务栏小狼毫图标 → **扩展设置**：

- **亚克力磨砂效果**：快速切换材质，状态自动保存，不需要重新部署。
- **更多设置…**：打开“小狼毫设置”的“外观”页。后续配置可增加页面，不增加菜单嵌套。

原有输入法设定、词典、同步、文件夹、部署等菜单保留。原来的界面风格选择步骤也进入同一外观页，避免两个入口修改不同配色。

## 成组设置与单独设置

外观页只编辑当前模式：勾选“亚克力磨砂效果”时编辑磨砂候选框，取消勾选时编辑普通实色候选框。切换模式会保留各自尚未应用的配色草稿。“成组设置”一次选择浅色、深色组合，“单独设置”分别选择两项，两种方式共用预览。Windows **应用模式**决定当前使用哪一个；任务栏颜色和强调色不参与判断。新安装默认两种模式均使用基础配色 Fluent。

每次打开页面都重新读取安装目录和用户目录的原始 `weasel.yaml` 及用户 `weasel.custom.yaml`。列表收起时只显示方案名称，展开时按**基础配色**和**自定义配色**分组。基础目录包含安装包方案及用户基础文件中的方案，自定义目录仅包含 custom 中的定义；整表、逐方案路径和 `preset_color_schemes/+` 追加写法都会读取。两类同名方案分别保留，预览和实际候选框使用所选来源的颜色；自定义定义不会覆盖基础目录。分组标题不能单独选为方案。

成组列表只列同一来源中完整的浅深配对：优先读取 `color_scheme_groups`，同时识别具有相同前缀的 `_light`／`_dark` 完整 ID 对，不凭颜色亮度猜测。单独设置的浅色列表隐藏明确为深色的方案，深色列表隐藏明确为浅色的方案；未标明主题的旧方案出现在两侧。主题属性优先取明确配对，其次 `variant: light/dark`，最后规范 ID 后缀。没有另一半的已知主题方案也能在对应列表选择。旧配置若已有跨主题选择，则保留该项并标明其主题，不自动替换。暂时不可用的旧值同样保留，直到用户重新选择。

两列预览只展示当前模式的浅色和深色候选框，包含圆角、边框、候选编号、文字和首选高亮。颜色读取所选方案，圆角和边框宽度读取已部署的全局布局；预览尺寸和候选内容固定，不等同于所有自定义布局的完整复现。磨砂使用固定的柔和背景和半透明颜色作示意，普通模式使用实色；不会读取桌面画面或启用真实输入法候选窗口。预览草稿不改变实际输入模式。

**确定**保存并关闭；**应用**保存后保留窗口，无改动时不可点击；**取消／Esc／关闭窗口**不保存尚未应用的草稿。此前已成功应用的设置不会被取消撤销。“恢复默认配色”只把当前模式的浅色和深色恢复为基础 Fluent，保留另一模式的选择和磨砂开关；点击确定或应用后生效，不删除用户自行定义的配色、字体和布局。

## 四个配置项

安装包基础配置中的默认值：

```yaml
style:
  color_scheme: Fluent_light
  color_scheme_dark: Fluent_dark
  color_scheme_acrylic: Fluent_light
  color_scheme_acrylic_dark: Fluent_dark
```

普通模式复用原有 `color_scheme`／`color_scheme_dark`，恢复原有的输入方案（schema）配色优先规则。磨砂模式有效的显式选择覆盖全局及 schema 的配色；缺失、空或无效时，沿用原有对应主题的解析结果，不用另一明暗项替代。配色选择仅覆盖颜色；磨砂仍有材质所需的背景透明度与阴影处理。

手动修改时，在自己的 `weasel.custom.yaml` 的 `patch:` 下设置 `"style/color_scheme"` 等同名路径。设置页也写这些键，没有第二套注册表配色。用户如果在 schema 中单独指定普通模式配色，该方案仍可覆盖全局普通配色。

设置页保存实际修改的方案名，并在对应键后附加 `_source` 记录 `base` 或 `custom`，例如 `style/color_scheme_source: base`。这只记录来源，不复制、改名或覆盖任何配色定义。来源未指定时兼容已有配置；指定来源不存在或文件读取失败时，实际候选框沿用部署后的可用配置。旧 `color_scheme_normal`／`color_scheme_normal_dark` 继续兼容读取；用户修改对应普通配色时，才迁移该项到原有键并移除其旧平铺／嵌套 normal 项。schema 原有优先级仍保留。整个配色表的历史 patch 也原样保留。

保存前核对三份来源文件有无外部改动，并为已有 custom 创建独立的 `.before-appearance-时间戳.bak`；打开窗口或取消不会写入。手工修改后的方案在下次打开页面时即可列出，实际输入效果仍在应用／部署后更新。修改的配色一次保存、一次部署；仅切换材质不需要部署。快速连续保存碰到 librime 的同秒时间戳时，会等该秒结束后再写入，避免保存成功却未重建。

## 添加自定义配色组

在现有 `patch:` 下逐项添加，不要替换整个 `preset_color_schemes`：

```yaml
patch:
  "preset_color_schemes/my_light":
    name: 我的浅色
    back_color: 0xffffff
    text_color: 0x202020
  "preset_color_schemes/my_dark":
    name: 我的深色
    back_color: 0x202020
    text_color: 0xffffff
  "color_scheme_groups/my_palette":
    name: 我的配色
    light: my_light
    dark: my_dark
```

下次打开页面时，“我的配色”作为一个组合显示。浅／深角色由该组的两个明确引用决定，需由配色作者保证它们适合对应主题。custom 中只修改某些颜色字段的方案，预览与显式来源选择均按这些字段和标准缺省值绘制，不借用同名基础方案的颜色。

## 安装包与升级

受版本管理的 `data/weasel.yaml` 包含原基础方案、Fluent 两个方案、配对关系和四项默认选择。默认字体使用 Windows 自带的 Microsoft YaHei；默认布局使用已验证范围内的 1 像素边框与 11 像素圆角配置。

构建数据时将该文件复制到输出目录；NSIS 安装器直接取这份源文件，避免混入旧的生成文件。安装器明确排除所有 `*.custom.yaml`，不附带个人用户配置。首次部署无需配色选择向导；原输入方案选择流程保留。升级保留已有 custom 文件与词库，用户保存设置时才写入自己的覆盖项。

基础配置保持原版本号，避免仅因加入 Fluent 就触发 librime 对同版本用户基础文件的淘汰规则。使用独立用户 `weasel.yaml` 的旧安装可能继续沿用旧默认值；在新外观页选择基础 Fluent 即可应用。程序从安装包基础目录读取所选方案，不把基础定义复制进用户 custom。

## 四边任务栏

原生菜单每次打开都读取触发位置所在显示器的工作区，不缓存任务栏方位。主菜单在左侧任务栏向右对齐、右侧任务栏向左对齐，顶部向下、底部向上；子菜单最终位置由 Windows 根据实际可用空间安排。不再用 `MFT_RIGHTORDER` 强制反向文字和箭头布局。使用图标区域作为排除区域，保留系统边界调整；支持负坐标的副屏和自动隐藏时的位置回退。不修改 Windows 的全局菜单／左右手偏好。

语言栏由系统托管的 `ITfMenu` 入口仍由 Windows 决定最终位置；移除深层配色菜单后只保留一层扩展设置。原生语言栏右键和独立托盘使用同一位置算法。四方向、不同缩放、多显示器和系统托管入口仍需新构建实机验收。

磨砂开关仍保存在当前用户 `Software\Rime\Weasel\UserSettings` 的 `AcrylicEnabled`，32／64 位组件共用相同注册表视图。首次使用默认开启，读取异常关闭可选材质；通知与已有候选刷新机制保留。

## 圆角修正的适用范围

普通未打包应用默认使用经过验证的 child 裁剪路径，不需要 Word 专用诊断环境变量。候选框宽高随布局更新。边缘对齐修正目前支持：缩放后的 1 像素不透明边框、3–17 像素圆角、两边尺寸均大于圆角直径。其他笔宽、透明边框、压缩圆角使用完整裁剪。

共享 WeaselServer 后台租约、打包应用和显式 R22 诊断保持现有材质路径。新修正没有更改前景 GDI 绘制、候选文字、原有 region 的 `+1` 或自定义皮肤参数。该范围的原生物理像素覆盖已检查；不同显示设备、全部应用和全部生命周期仍需要实际回归，不等同于所有渲染路径均已修复。
